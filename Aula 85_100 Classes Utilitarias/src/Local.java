import java.util.Locale;

public class Local {
    public static void main(String[] args) {
        Locale locale = Locale.getDefault();
        System.out.println("\nLocal padrão " + locale);
    }
}
